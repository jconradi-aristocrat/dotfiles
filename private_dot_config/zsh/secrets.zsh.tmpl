# Secrets module — populated from Bitwarden at chezmoi apply time.
# Add `export FOO=...` lines using vault references, e.g.:
#   export OPENAI_API_KEY={{ (bitwarden "item" "openai-key").login.password }}
# Leave empty if you don't need any env-var secrets.
